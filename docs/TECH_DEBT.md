# Технический долг и области для пересмотра

Документ фиксирует известные проблемные области кода, которые работают, но желательно пересмотреть в будущем.

**Версия:** 0.1.0  
**Дата:** 2026-02-10

---

## Градация проблемности

| Уровень | Описание | Когда решать |
|---------|----------|--------------|
| 🔴 **Высокий** | Источник багов, сложно поддерживать | При первой возможности |
| 🟡 **Средний** | Неудобно, но работает | При рефакторинге области |
| 🟢 **Низкий** | Можно улучшить, но не критично | Когда будет время |

---

## 🔴 Высокий приоритет

### 1. Обновление панелей через innerHTML

**Файл:** `render/src/scenes/visitor.js`  
**Строки:** ~1500-1700, 1900-2100

**Проблема:**
```javascript
content.innerHTML = html;
bindHighlightWidgets(content);
bindEmblemSwap(content);
```

- Каждый раз пересоздаётся весь DOM
- Вручную навешиваются обработчики (легко забыть)
- Теряется состояние (scroll, focus)
- Дублирование кода bind-функций

**Решение:** Перейти на React-компоненты для панелей. Заготовки уже есть в `render/src/ui/`.

**Когда:** Когда появятся баги с потерей состояния или забытыми bind-вызовами.

---

### 2. Монолит visitor.js (3800+ строк)

**Файл:** `render/src/scenes/visitor.js`

**Проблема:**
- Сложно найти нужную функцию
- Высокая связность (всё зависит от всего)
- Сложно тестировать изолированно

**Решение:** Разбить на модули:
- `graph-data.js` — загрузка и фильтрация данных
- `graph-render.js` — рендеринг узлов и связей
- `panels.js` — логика панелей Story/System/Service
- `widgets.js` — поведение виджетов
- `navigation.js` — навигация и scene stack

**Когда:** При следующем крупном рефакторинге.

---

## 🟡 Средний приоритет

### 3. Глобальные переменные состояния

**Файл:** `render/src/scenes/visitor.js`  
**Строки:** ~130-200

**Проблема:**
```javascript
let currentStep = null;
let hoveredWidgetId = null;
let activeRootLever = null;
// ... 20+ глобальных переменных
```

- Сложно отследить, кто и когда меняет состояние
- Нет единого источника истины
- Сложно отлаживать

**Решение:** 
- Объединить в объект состояния
- Или перейти на React state/context

**Когда:** При переходе панелей на React.

---

### 4. Дублирование шаблонов виджетов

**Файл:** `render/src/scenes/visitor.js`

**Проблема:** Один и тот же HTML-шаблон виджета повторяется 10+ раз:
```javascript
`<div class="domain-widget highlight-widget" data-node-id="${nodeId}">
  <div class="widget-frame">
    ${getWidgetImageHtml(...)}
  </div>
</div>`
```

**Решение:** Вынести в функцию `renderWidget(nodeId, type, options)`.

**Когда:** При добавлении нового типа виджета.

---

### 5. Неиспользуемый код lever механики

**Файл:** `render/src/scenes/visitor.js`  
**Строки:** ~68-71, 193-203, 2760-2805

**Проблема:** Переменные и функции lever остались после удаления механики:
- `ROOT_LEVER_CONFIG`
- `activeRootLever`
- `activeLeverWidgetId`
- `setRootLeverState()`

**Решение:** Удалить полностью или пометить `@deprecated`.

**Когда:** При следующей чистке кода.

---

## 🟢 Низкий приоритет

### 6. CSS дублирование

**Файл:** `render/src/scenes/visitor.css`

**Проблема:** Похожие стили для разных типов виджетов.

**Решение:** Использовать CSS-переменные или SCSS mixins.

**Когда:** При редизайне виджетов.

---

### 7. Отсутствие типизации

**Проблема:** JavaScript без TypeScript — нет автодополнения, легко ошибиться в именах свойств.

**Решение:** Постепенно добавлять JSDoc или перейти на TypeScript.

**Когда:** При росте команды или сложности.

---

### 8. Большой бандл (1.5MB)

**Файл:** `render/dist/assets/visitor-*.js`

**Проблема:** Vite предупреждает о размере чанка.

**Решение:** 
- Code splitting через dynamic import
- Вынести Three.js в отдельный чанк

**Когда:** Когда скорость загрузки станет проблемой.

---

## Не трогать (работает хорошо)

| Область | Почему не трогать |
|---------|-------------------|
| CSS анимации виджетов | Простые, работают, не требуют JS |
| Three.js рендеринг графа | Изолирован, стабилен |
| Pointer-tags система | Чистая, расширяемая |
| UI Rules проверки | Автоматизированы, ловят регрессии |

---

## Как использовать этот документ

1. **Перед рефакторингом** — проверить, есть ли область в списке
2. **При баге** — проверить, связан ли с известной проблемой
3. **При планировании** — учитывать приоритеты

Обновлять документ при обнаружении новых проблем или решении существующих.
