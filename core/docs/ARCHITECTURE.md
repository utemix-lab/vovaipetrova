# Архитектура Vovaipetrova

> Единый онтологический граф — единственный источник истины

---

## Обзор

Система построена на принципе **единого канонического графа**:

```
┌─────────────────┐    universe.json    ┌─────────────────┐
│   core/         │ ──────────────────► │   data/         │
│   Ядро системы  │                    │   Данные графа   │
└─────────────────┘                    └─────────────────┘
                                              │
                                              ▼
┌─────────────────┐    3D рендер        ┌─────────────────┐
│   render/       │ ◄────────────────── │   data/         │
│   Визуализация  │                    │   Ассеты и UI   │
└─────────────────┘                    └─────────────────┘
```

---

## Ключевые принципы

### 1. Единый источник истины

`data/graph/universe.json` — единственный файл, определяющий структуру.

### 2. Разделение ответственности

| Компонент | Роль | Права |
|-----------|------|-------|
| **core** | Документация и валидация | READ |
| **data** | Хранение данных | READ/WRITE через editor |
| **render** | Визуализация | READ-ONLY |

### 3. Views — не графы

Граф один. Views — это фильтры:

| View | Показывает |
|------|------------|
| Knowledge | domain, concept, character |
| System | root, hub, module, spec, process, policy |
| All | все узлы |

---

## Node Contract v1

Каждый узел — онтологическая сущность:

```json
{
  "id": "уникальный-id",
  "label": "Отображаемое имя",
  "type": "concept",
  "visibility": "public",
  "status": "expandable",
  "semantics": {
    "role": "content",
    "abstraction": "medium"
  },
  "story": "Нарративный контент",
  "system": "Техническое описание",
  "service": "Доступные действия"
}
```

---

## Пути и зависимости

Все пути определены в `render/src/compat/paths.js`:

```javascript
export const PATHS = {
  DATA_ROOT: "/vovaipetrova/data",
  GRAPH: "/vovaipetrova/data/graph/universe.json",
  ASSETS: "/vovaipetrova/data/assets"
};
```

---

## Текущий статус

| Компонент | Статус |
|-----------|--------|
| Universe Graph | ✅ Реализован |
| Editor | ✅ Работает |
| 3D Render | ✅ Работает |
| Validator | 📋 Планируется |
| Tests | 📋 Планируется |

---

## Что нельзя делать

❌ Заводить второй "граф графа"  
❌ Редактировать канон из рендера  
❌ Хардкодить пути  
❌ Смешивать narратив с архитектурой

---

## Короткая формула

> Система с единым онтологическим графом, где знания и сама система являются узлами одного канона.
