/**
 * 🎯 МЕТА-СИСТЕМА: Рефлексивная агентность
 *
 * УРОВНИ СТЕКА:
 * 1. Код (TypeScript/Python/Rust) ← МЫ ЗДЕСЬ СЕЙЧАС
 * 2. Граф зависимостей (AST, CodeQL)
 * 3. Онтологический граф (OWL/RDF) ← ЦЕЛЬ ВИЗУАЛИЗАЦИИ
 * 4. Слой связывания (мост аннотация→код)
 * 5. Агентный движок (LLM + RAG)
 *
 * ПРИНЦИП РЕФЛЕКСИИ:
 * Система ставит себе диагноз → генерирует план → проверяет результат
 *
 * ТЕКУЩИЙ ЭТАП:
 * Визуализация онтологического графа в 3D как тренинг архитектурных паттернов.
 * Каждая анимация/состояние UI → прообраз состояния в мета-системе.
 */
export const ARCHITECTURE_CONTEXT = {
  vision: "Рефлексивная система с гомеостазом замысла",
  currentPhase: "Визуальный прототип как архитектурный тренинг",
  nextStep: "React для сложных анимаций → перенос паттернов на AST-графы",
  constraints: [
    "3D-граф на Three.js — самостоятельная физическая система",
    "UI — DOM поверх (двухслойная архитектура)",
    "Связь через события и shared state (universe.json)"
  ]
} as const;
