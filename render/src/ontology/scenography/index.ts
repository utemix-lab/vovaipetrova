/**
 * @fileoverview Сценография — декларативный слой интерактивной логики
 * 
 * БУДУЩИЙ МОДУЛЬ. Сейчас это placeholder для якорей.
 * 
 * Сценография описывает:
 * - Как окна реагируют на контекст (композиция)
 * - Правила подсветки и навигации
 * - Систему проекций (что показывать, когда, как)
 * 
 * Цель: вывести логику из императивного кода visitor.js
 * в декларативный формат, чтобы система могла:
 * 1. "Объяснить" свои решения (самопонимание)
 * 2. Служить материалом для Cryptocosm (мозг понимает тело)
 * 3. Генерировать документацию автоматически
 * 
 * Формализация произойдёт задним числом, когда логика стабилизируется.
 * 
 * @ArchProto(
 *   futureLayer: "SCENOGRAPHY_LAYER",
 *   patterns: ["Declarative UI logic", "Self-explaining system"],
 *   status: "placeholder"
 * )
 */

// ═══════════════════════════════════════════════════════════════════════════
// ТИПЫ (БУДУЩИЕ)
// ═══════════════════════════════════════════════════════════════════════════

/**
 * Правило композиции окон для типа узла.
 * @future
 */
export interface SceneCompositionRule {
  nodeType: string;
  storyPanel: string;   // Что показывать в Story
  systemPanel: string;  // Что показывать в System
  servicePanel: string; // Что показывать в Service
}

/**
 * Правило подсветки.
 * @future
 */
export interface HighlightRule {
  trigger: string;      // Что вызывает подсветку
  target: string;       // Что подсвечивается
  style: string;        // Как подсвечивается
}

/**
 * Правило навигации.
 * @future
 */
export interface NavigationRule {
  from: string;         // Откуда
  to: string;           // Куда
  trigger: string;      // Что вызывает переход
}

// ═══════════════════════════════════════════════════════════════════════════
// PLACEHOLDER EXPORTS
// ═══════════════════════════════════════════════════════════════════════════

/**
 * Заглушка для будущего API.
 * Будет заполнена при формализации сценографии.
 */
export const SCENOGRAPHY_VERSION = "0.0.1-placeholder";

/**
 * Проверка готовности модуля.
 */
export function isScenographyReady(): boolean {
  return false; // Пока не готово
}
