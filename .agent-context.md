# 🧠 КОНТЕКСТ АГЕНТА: РЕФЛЕКСИВНАЯ СИСТЕМА

## ТЕКУЩАЯ АРХИТЕКТУРА

1. **3D-граф**: Three.js + force-graph (физическая симуляция)
2. **UI-слой**: DOM поверх (управление, панели)
3. **Связь**: события + shared state (`universe.json`)

## БУДУЩАЯ АРХИТЕКТУРА (цель)

1. Тот же граф, но для AST и OWL-графов
2. UI → интерфейс для LLM-агента
3. События → триггеры для рефлексии системы

## ПРИНЦИП РАБОТЫ С КОДОМ

- Каждая анимация → прообраз состояния мета-системы
- Каждый компонент → тренировка для семантических модулей
- Каждое событие → модель для агентного взаимодействия

## СЛЕДУЮЩИЕ ШАГИ

1. Внедрить React для сложных анимаций
2. Выделить абстрактный GraphEngine из Three.js
3. Подготовить слои для подключения AST-парсера

Ключ: контекст должен быть в коде (комментарии, типы, константы) и коротко в отдельном файле, который агент читает перед работой.
