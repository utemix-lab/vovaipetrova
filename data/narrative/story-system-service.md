# SINGLE SOURCE PLAYBOOK

> Единый документ замысла системы для быстрого старта с любым агентом.

**Версия:** 1.0  
**Дата:** 2026-01-25

---

## 0. Цель (ключевая, не терять)

Построить **самоописывающуюся систему**, основанную на едином каноническом графе, которая:

- имеет **единый источник истины** (SSoT)
- может **визуализироваться** разными рендерами
- может быть **прочитана и понята самой системой** (через RAG)
- работает **без субъектности и магии**, только через структуру, контракты и валидацию

### Что значит "понимать себя"

Система должна:
- знать, из каких частей она состоит
- какие у этих частей роли
- какие связи допустимы
- уметь проверить собственную целостность

---

## 0.1. Playbook для IDE-агентов

Если кодер меняется или нет полного контекста, начни здесь:

- `../MACHINE_CONTEXT.md`
- `AGENT_PLAYBOOK.md`
- последний checkpoint в `docs/checkpoints/`

---

## 1. Канон: Universe Graph (SSoT)

### 1.1. Что является истиной

**Единственный источник истины** — `universe.json`.

В нём:
- узлы (nodes)
- связи (edges)
- минимальные метаданные узлов (Node Contract v1)
- ссылки на контент (Story/System/Service) — напрямую или через refs

**Никакой другой файл не определяет структуру системы.**

### 1.2. Node Contract v1 (инвариант)

Каждый узел в universe.json — это **онтологическая сущность**, а не "контент".

#### Минимальный контракт узла:

| Поле | Значения | Дефолт |
|------|----------|--------|
| `id` | string (глобально уникальный, вечный) | — |
| `label` | string | — |
| `type` | root, hub, domain, concept, character, module, spec, process, policy, artifact | concept |
| `visibility` | public, internal, hidden | public |
| `status` | core, expandable, frozen, deprecated | expandable |
| `semantics.role` | description, container, mediator, content | content |
| `semantics.abstraction` | high, medium, low | medium |
| `rag.index` | boolean | true |
| `rag.priority` | 0–1 | 0.5 |

Все поля имеют дефолты. Старые узлы валидны через нормализацию.

### 1.3. Edges (связи)

Граф один, типы связей различаются:

| Тип | Назначение |
|-----|------------|
| `structural` | скелет системы |
| `contains` | контейнер → элемент |
| `relates` | смысловая связь |
| `describes` | описывает |
| `implements` | реализует |
| `renders` | визуализирует |

> **Нет второго графа.** Есть один граф и разные способы его читать.

---

## 1.1. Режим фиксации (памятка)

- Систему придумываем на ходу, фиксируя сдвиги и перспективы.
- Это не линейный план действий, а эволюционная фиксация.
- Query Mode = глобальный режим восприятия, сейчас живёт в Service.
- Фиксация интерфейсных режимов преждевременна до появления второго режима.
- Естественный триггер для пересмотра: второй режим-конкурент (Simulation/Timeline/Practice/Persona).
- По завершении этапа фиксируем ADR (коротко, без усложнений).

---

## 2. Репозитории и их роли

### 2.1. extended-mind — мозг и интроспекция

**Роль:**
- редактор канона
- хранитель онтологии
- место ручной эволюции системы

**Факты:**
- `editor.html` редактирует `universe.json`
- extended-mind может писать канон
- позже: RAG/LLM-автоматизация, но тоже через этот модуль

### 2.2. dream-graph — рендер (витрина)

**Роль:**
- чистый визуализатор
- **никогда не источник истины**

**Принцип:**
```
universe.json + contracts → deterministic scene
```

dream-graph:
- читает канон
- уважает `source=canon|demo`
- поддерживает views (фильтры), но не меняет граф

**Два режима:**
- `visitor.html` — витрина (деплоится)
- `index.html` — лаборатория экспериментов (не деплоится)

### 2.3. contracts — язык между рендерами

**Роль:**
- UI-контракты (layout / interaction / bindings)
- ассеты и тексты
- sessions (состояния)

> **Важно:** contracts не определяет структуру системы, он только интерпретирует канон.

### 2.4. godot-sandbox — опциональный

- экспериментальный рендер / "строительные леса"
- read-only consumer контрактов
- может быть удалён без изменения канона
- существует для поиска паттернов сценографии

---

## 2.5. Operational Sync (обязательные правила)

### Контракты → рендер

- Канон и ассеты живут в `contracts/contracts/public` (SSoT).
- Рендер и Pages используют **снапшот**: `dream-graph/public/contracts/public`.
- Синхронизация выполняется вручную:
```
dream-graph\scripts\sync-contracts.ps1
```
- Симлинки запрещены — только явный sync.

### HF Space (генератор TG)

- Исходник: `extended-mind/spaces/telegram-content`
- Деплой: `temp-hf-space` (локальный клон Space)
- Синхронизация:
```
extended-mind\scripts\sync-hf-space.ps1
```
- После синка: `git commit && git push` в `temp-hf-space`

---

## 3. Views (проекции), а не "несколько графов"

**Граф один.** Разные — режимы просмотра.

| View | Показывает |
|------|------------|
| **Knowledge** | domain, concept, character |
| **System** | root, hub, module, spec, process, policy |
| **All** | все узлы |

Views = фильтры + визуальные правила. **Никаких копий графа.**

---

## 4. System Graph (самоописание системы)

Документация — не "вне системы".

В каноне существует ветка **System Graph**, которая описывает саму систему:

```
system (root)
└── system-graph (hub)
    ├── module-extended-mind (module)
    ├── module-dream-graph (module)
    ├── module-contracts (module)
    ├── policy-ssot (policy)
    └── process-edit-to-render (process)
```

Эти узлы:
- имеют `type = module | policy | process`
- ссылаются на README / ARCHITECTURE через `content.system_ref`
- видны в System view

**Так система "знает себя".**

---

## 5. Структура vs Состояние (жёсткое разделение)

| Структура (канон) | Состояние (не канон) |
|-------------------|----------------------|
| nodes, edges | camera, selection |
| type, role, semantics | layout positions |
| content refs | UI session data |

Состояние хранится в:
```
contracts/public/sessions/*
```

**Рендеры не пишут структуру.**

---

## 6. Самоосознание без субъектности (ключевой принцип)

Система "осознаёт себя" не через тексты, а через:

1. **Канон** — формальное описание структуры
2. **Инварианты** — правила, которые нельзя нарушать
3. **Валидацию** — способность проверить себя
4. **Retrieval (RAG)** — способность ответить на вопросы о себе, читая канон

**Никакой "личности".** Только структура + проверка + извлечение.

---

## 7. 3S-окна: театральная песочница, а не проекции графа

### 7.1. Критическое различие

В системе есть два разных понятия, которые легко перепутать:

| Понятие | Что это |
|---------|---------|
| **Views (проекции)** | способы просмотра одного канонического графа — структурные линзы |
| **3S-окна (Story/System/Service)** | сценография и театральная подача контента — режиссура и перформанс |

**Эти вещи нельзя отождествлять.**

### 7.2. Два режима устойчивости

#### A) Стабильный режим: Канон (Universe Graph)

- единый источник истины: `universe.json`
- узлы, рёбра, Node Contract v1
- строгая валидируемая структура
- views — это фильтры, а не новые графы

#### B) Экспериментальный режим: 3S-сценография

- смысл и форма окон Story/System/Service **не зафиксированы**
- 3S — главная зона художественного и исследовательского эксперимента
- цель — уйти от классического веб-интерфейса к театральному действию
- **3S нельзя "застывать" в архитектуре**

> Архитектура должна защищать канон от экспериментов 3S, а не наоборот.

### 7.3. Чем 3S-окна являются — и чем не являются

#### 3S-окна — НЕ:
- ❌ не проекции графа
- ❌ не второй граф
- ❌ не онтология системы
- ❌ не структурное описание системы

#### 3S-окна — ЭТО:
- ✅ сценическая интерпретация канонических данных узла
- ✅ перформативный слой: нарратив, технический контекст, действие
- ✅ лаборатория сценографии: ритм, внимание, переходы, присутствие
- ✅ "театр без актёров"

> Можно думать о 3S как о **сцене, подключённой к канону**.

### 7.4. Ключевой контракт: канон даёт указатели, песочницы интерпретируют

**Правило:**
```
Канон (universe.json) хранит минимальные данные и ссылки.
Рендер / песочница решает, как именно это становится 3S-действием.
```

#### Узел канона может иметь:
- `content.story_ref`
- `content.system_ref`
- `content.service_ref`

Эти ссылки указывают на тексты (markdown и т.п.) или остаются inline (временно).

#### Канон НЕ определяет:
- расположение окон
- поведение панелей
- театральную логику
- переходы, звук, свет
- "что сегодня значит Story / System / Service"

**Всё это — уровень сценографии.**

### 7.5. "Театр без актёров" — что мы реально исследуем

Это не стандартная задача UI/UX. Это исследовательский вопрос:

- как граф становится сценой?
- как выбор узла превращается в смену сцены?
- как управлять вниманием без кнопок?
- какой минимальный "язык режиссёра" нужен системе?

**Ответы не должны быть зашиты заранее.**

---

## 8. Главное, что нельзя делать

❌ заводить второй "граф графа"  
❌ редактировать канон из рендеров  
❌ смешивать документацию с каноном без узлов System Graph  
❌ хранить истину в README вместо universe.json  
❌ заводить отдельный "граф 3S"  
❌ кодировать сценографию в каноне  
❌ путать 3S с Knowledge/System views  
❌ превращать песочницы в источник истины

---

## 9. Текущий статус

| Компонент | Статус |
|-----------|--------|
| Canon loader | ✅ Реализован |
| Node Contract v1 | ✅ Внедрён |
| Views | ✅ Работают |
| System Graph | ✅ Добавлен |
| Validator (CLI self-check) | 📋 Планируется |
| Content refs | 📋 Планируется |
| RAG | 📋 В перспективе |

---

## 10. Рекомендуемый порядок действий

1. **Держать канон и views стабильными** (это уже сделано)
2. **Экспериментировать с 3S в песочнице:**
   - выбор узла → загрузка его 3S-контента → сценическое представление
3. **Осмыслить найденные паттерны**
4. **Только потом решать**, нужен ли Godot или нет

---

## 11. Структура репозиториев

### extended-mind (ядро)
```
extended-mind/
├── docs/
│   ├── SINGLE-SOURCE-PLAYBOOK.md  # ← Этот файл
│   ├── ARCHITECTURE.md
│   ├── UNIVERSE_GRAPH_SPEC.md
│   ├── ROADMAP.md
│   └── _archive/
├── README.md
└── LICENSE
```

### contracts (данные)
```
contracts/public/
├── graph/
│   ├── universe.json    # 🎯 SSoT
│   └── editor.html
├── assets/
├── texts/
├── sessions/
└── ui/
```

### dream-graph (витрина)
```
dream-graph/
├── src/scenes/
│   ├── visitor.js       # 🎯 Витрина (деплоится)
│   └── *.js             # 🧪 Эксперименты
├── visitor.html
└── index.html
```

### godot-sandbox (опционально)
```
godot-sandbox/           # Строительные леса
```

---

## 12. Короткая формула

> Это система с единым онтологическим графом, где и знания, и сама система являются узлами одного канона, а все интерфейсы — лишь проекции этого канона.

> 3S-окна — не проекции графа, а театральная песочница для экспериментов с подачей канонического контента.

---

## 13. Чек-лист для агента

При начале работы проверь:

- [ ] `universe.json` существует и валиден
- [ ] Изменения структуры — только через extended-mind
- [ ] Рендеры (dream-graph, godot) — read-only
- [ ] Views — фильтры одного графа, не копии
- [ ] 3S — эксперимент, не архитектура
- [ ] Состояние отдельно от структуры

---

**Конец документа.**
