# Vovaipetrova Repository Setup

## 🎉 Новый репозиторий создан!

### Структура проекта:
```
vovaipetrova/
├── README.md                    # Основное описание
├── package.json                 # Корневые скрипты
├── LICENSE                      # MIT лицензия
├── .gitignore                   # Игнорируемые файлы
├── .github/workflows/ci.yml     # CI/CD pipeline
│
├── core/                        # Ядро системы
│   ├── docs/
│   │   ├── MACHINE_CONTEXT.md   # Контракт для агентов
│   │   ├── ARCHITECTURE.md      # Архитектура системы
│   │   └── QUICK_START.md       # Быстрый старт
│   └── validate.js              # Валидатор графа
│
├── data/                        # Данные и нарратив
│   ├── graph/
│   │   ├── universe.json        # Единственный источник истины
│   │   └── editor.html          # Графовый редактор
│   ├── narrative/
│   │   ├── characters.md        # Персонажи
│   │   ├── roadmap.md           # Дорожная карта
│   │   └── story-system-service.md # Концепция 3S
│   └── assets/                  # Изображения, ассеты
│
└── render/                      # 3D визуализация
    ├── src/
    │   ├── compat/paths.js      # Compatibility layer
    │   └── scenes/              # Сцены визуализации
    ├── public/
    │   ├── visitor.html         # Продакшн рендер
    │   └── lab.html             # Лаборатория
    └── package.json             # Render зависимости
```

### ✅ Что готово:
- [x] Структура папок
- [x] Критичная документация (3 файла)
- [x] Валидатор universe.json
- [x] Compatibility layer для путей
- [x] CI/CD pipeline
- [x] Исправленные пути в коде
- [x] Нарратив сохранён

### 🚀 Следующие шаги:

#### 1. Создать репозиторий на GitHub:
```bash
# На GitHub: New repository → vovaipetrova
# Не добавлять README, .gitignore, license
```

#### 2. Инициализировать git:
```bash
cd r:\vovaipetrova
git init
git add .
git commit -m "Initial commit: unified vovaipetrova repository"
```

#### 3. Подключить к GitHub:
```bash
git remote add origin git@github.com:utemix-lab/vovaipetrova.git
git branch -M main
git push -u origin main
```

#### 4. Настроить GitHub Pages:
- Settings → Pages
- Source: Deploy from branch
- Branch: main → / (root)
- Папка: `render/dist` (после первого билда)

#### 5. Установить зависимости:
```bash
npm install
cd render && npm install && cd ..
```

#### 6. Проверить работу:
```bash
npm run validate    # Валидация графа ✅
npm run dev        # Запустить редактор + рендер
```

### 📊 Результат:
- **Файлов:** ~50 (vs 300+ в старых репозиториях)
- **Документация:** 3 ключевых файла (vs 66+)
- **Архитектура:** Чистая, инженерная
- **Пути:** Централизованные в compatibility layer
- **CI/CD:** Современный GitHub Actions

### 🔧 Команды:
```bash
npm run dev          # Запустить всё
npm run dev:render   # Только 3D визуализация  
npm run dev:editor   # Только редактор графа
npm run validate     # Проверить структуру
npm run build        # Собрать render
npm run deploy       # Деплой на Pages
```

### 📝 Ключевые принципы:
1. **Единый источник истины:** `data/graph/universe.json`
2. **Разделение ответственности:** core/data/render
3. **Централизованные пути:** `render/src/compat/paths.js`
4. **Минимализм:** только необходимое
5. **Инженерный подход:** валидация, CI/CD, тесты

**Новый репозиторий готов к работе!** 🚀
