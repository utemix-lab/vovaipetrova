{
  "name": "vovaipetrova",
  "version": "1.0.0-alpha",
  "description": "Единая система знаний на основе онтологического графа",
  "type": "module",
  "scripts": {
    "dev": "cd render && npm run dev",
    "build": "cd render && npm run build",
    "deploy": "npm run build && npx gh-pages -d render/dist",
    "release": "git push origin main && npm run deploy"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  },
  "keywords": [
    "ontology",
    "graph",
    "knowledge",
    "3d-visualization"
  ],
  "author": "utemix-lab",
  "license": "MIT"
}
